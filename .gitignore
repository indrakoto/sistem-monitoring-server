# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env* !.env.example # Tambahkan ini jika Anda memiliki file .env.example untuk konfigurasi

# vercel
.vercel

# typescript
*.tsbuildinfo
# next-env.d.ts - DIHAPUS. File ini harus di-commit agar TypeScript berfungsi dengan baik di lingkungan lain.

# Prisma (sudah ada yang ini)
/src/generated/prisma

# === PENAMBAHAN SPESIFIK UNTUK PRISMA & SQLITE ===

# Prisma Client (Biasanya tidak perlu di-commit jika tidak di-generate ke folder khusus)
# Jika Prisma Client di-generate di node_modules, ini sudah dicakup di atas.

# SQLite Database Files
*.db
*.sqlite
/prisma/*.db # Abaikan file database di folder prisma
/prisma/*.sqlite